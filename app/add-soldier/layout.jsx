export async function generateMetadata({ params }) {
  return {
    title: `הוספת חייל | לזכרם`,
    description: "לזכר הנופלים",
  };
}

export default function Layout({ children }) {
  return children;
}
