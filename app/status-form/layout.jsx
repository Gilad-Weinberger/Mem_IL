export async function generateMetadata({ params }) {
  return {
    title: `בקשת שדרוג סטטוס | לזכרם`,
    description: "לזכר הנופלים",
  };
}

export default function Layout({ children }) {
  return children;
}
