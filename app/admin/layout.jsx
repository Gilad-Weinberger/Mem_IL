export async function generateMetadata({ params }) {
  return {
    title: `ממשק מנהל | לזכרם`,
    description: "לזכר הנופלים",
  };
}

export default function Layout({ children }) {
  return children;
}
